# 配布用 標準キーマップ（`standard-keymap` ブランチ）

`main` のキーマップは作者の使い方に寄せた設定（コンボ限定の ESC/TAB、Z のホームロウ
モディファイア、日本語 IME 用の LANG1/LANG2、余りキーの F13〜F16）になっている。
このブランチは、**自作キーボードを触ったことがない人が、箱を開けてから日本語を打てる
ようになるまで自力で辿り着ける**ことを目的に、一般的なキー配列へ寄せたもの。
レイヤー 1〜4 / MOUSE / SCROLL の構成は `main` のまま。

## 受け取った人に渡すガイド

`keymap-guide.html` は、つなぎ方・基本レイヤーの配列・簡単な確認だけの1枚もの。
全レイヤーの詳しい配列は `keymap-drawer/mona2.svg`（CI が自動生成）に任せて、
ガイド側からはリポジトリへ誘導している。ブラウザで開くだけで読める。

## default_layer（レイヤー0）

```
          左手                                      右手
 row1   Q     W     E     R     T                     Y     U     I     O     P
 row2   A     S     D     F     G              ESC    H     J     K     L     ;
 row3   Z     X     C     V     B     TAB      DEL    N     M     ,     .     /
 row4   Ctrl  GUI   Shift BS    Ent*  英数*    かな*  Spc*                    Alt
```

- `Ent*` … タップで **Enter**、長押しで **layer_2**（記号レイヤー）
- `Spc*` … タップで **Space**、長押しで **layer_1**（数字レイヤー）
- `英数*` `かな*` … タップで **英数 / かな**（IME 切り替え）、長押しで **layer_3**（カーソル / ウィンドウ操作）。長押しは左右どちらでも同じ
- 左右の親指キーを**同時押し**すると **layer_4**（Bluetooth 設定・bootloader）
- 左手側のロータリーエンコーダー … 上下スクロール

## マウスクリック（オートマウスレイヤー）

トラックボールを動かすと、そこから **500ms のあいだ `MOUSE` レイヤー（layer 5）が自動で
有効**になり、指を動かさずにクリックできる。

```
 左手   S = 中クリック   D = 右クリック   F = 左クリック
 右手   J = 左クリック   K = 右クリック   L = 中クリック
```

`main` ではこの自動切り替え（`mona2.dtsi` の `&zip_temp_layer 5 500`）がコメントアウト
されており、`MOUSE` レイヤーへ入る手段が無く、クリックは「左親指 `Ent*` を長押ししながら
`J`」しか無かった。初見で気づけないため、このブランチでは有効化した。
`Ent*` 長押し中の `H`=中 / `J`=左 / `K`=右 も従来どおり使える。

打鍵中にボールへ触れて文字がクリックに化けるのを避けるため、`mona2.dtsi` で
`&zip_temp_layer` に `require-prior-idle-ms = <150>` を設定してある。直前に打鍵が
あった場合はマウスレイヤーが出ない。

**残る副作用**: ボールを動かして 500ms 以内に `S` `D` `F` `J` `K` `L` を打つと、文字では
なくクリックになる。気になる場合は `<&zip_temp_layer 5 500>` の数値を小さくするか、
行をコメントアウトに戻す。実機で試していないので、購入者に渡す前に確認してほしい。

## `main` からの変更点

| 位置 | main | このブランチ | 理由 |
| --- | --- | --- | --- |
| 右手 row2 内側 | `F13` | `ESC` | F13〜F16 は導通チェック用の埋め草で、一般の用途では何も起きない |
| 左手 row3 内側 | `F14` | `TAB` | 同上。TAB をコンボから実キーへ |
| 右手 row3 内側 | `F15` | `DEL` | 同上 |
| 左手 row4 3番目 | `F16` | `Shift` | Z のホームロウ mod を外したため、Shift の置き場所が必要 |
| 左手 row3 1番目 | `&mt LEFT_SHIFT Z` | `Z` | 押しっぱなしで Shift になる挙動は初見で戸惑うため解除 |
| 両親指 | `&lt_to_layer_0 3 LANG1/LANG2` | `&lt 3 LANG1/LANG2` | かな/英数 は残し、独自ビヘイビアを標準の `&lt` に置き換え |
| コンボ `S`+`D` | `TAB` | 削除 | TAB を実キーに出したため不要 |
| コンボ 両親指 | `&lt 4 ESC` | `&mo 4` | ESC を実キーに出したので、レイヤー4へ入る役割だけ残した |
| layer_1 の `A` 位置 | `&bt BT_PRV` | `&trans` | 作者個人の割り当てを撤去 |
| layer_2 の左下 | `&gresc` | `&trans` | 同上 |
| layer_3 の左右内側 | `&trans` | `&kp INT_MUHENKAN` / `&kp INT_HENKAN` | LANG1/LANG2 が効かない環境向けの 無変換 / 変換 |

`main` の `lt_to_layer_0` はタップ時に `&to 0`（レイヤー0へ戻る）を挟む独自ビヘイビア
だが、このブランチには `&to` でレイヤーを固定する箇所が無いため、標準の `&lt` で同じ
挙動になる。`lt_to_layer_0` の定義自体は残してあるので、元に戻すこともできる。

## 日本語入力の切り替え

`LANG1`（かな）/ `LANG2`（英数）は macOS の日本語キーボードにそのまま対応するが、
Windows では IME や設定によって反応しないことがある。そのため layer_3 の左右の内側キーに
`INT_MUHENKAN`（無変換）/ `INT_HENKAN`（変換）も置いた。親指キーを押しっぱなしにしながら
内側のキーを押す形で、どちらの環境でも切り替えられるようにしてある。

どちらが効くかは購入者の環境次第なので、**渡す前に手元の Windows / Mac で両方試して
おくこと**。私は実機で確認していない。

## 初回のペアリング

Bluetooth の一覧に出る名前は `mona2`（`Kconfig.defconfig` の `ZMK_KEYBOARD_NAME`）。
ホストと接続するのは central 側の右手側だけなので、購入者のホストからは**右手側の1台
しか見えない**。左手側は central と split ペアリングで直結しており、ホストの一覧には
出てこない。接続先は 5 プロファイル持てる（layer_4 の `Y`〜`P`）。

出荷前に `settings_reset` を当てた場合は自分のホストとのペアリングも消えるので、
**購入者に渡す直前に自分のペアリングを消しておく**とトラブルが少ない
（layer_4 の右下 = `&bt BT_CLR_ALL`）。

## 電源は左 → 右の順で入れる

`Kconfig.defconfig` のとおり右（`mona2_r`）が `ZMK_SPLIT_ROLE_CENTRAL`、左が
peripheral。central は起動時に peripheral を探しに行くので、**左を先に入れて待たせて
から右を入れる**。逆順だと左半分だけ反応しないことがあり、その場合は左を入れ直す。
ホストとの BLE 接続は右が担当する。

## ファームウェアの入手と書き込み

1. GitHub の **Actions → Build** から、このブランチの最新の成功したビルドを開く
2. Artifacts の `firmware` をダウンロードして展開する
3. 各 uf2 を書き込む
   - `mona2_l rgbled_adapter-seeeduino_xiao_ble-zmk.uf2` … 左手側
   - `mona2_r rgbled_adapter-seeeduino_xiao_ble-zmk.uf2` … 右手側（トラックボール側）
   - `settings_reset-seeeduino_xiao_ble-zmk.uf2` … 設定のリセット用（下記参照）
4. 書き込みは、XIAO のリセットボタンを素早く2回押してブートローダー（`XIAO-SENSE` ドライブ）
   を出し、uf2 をドラッグ＆ドロップ

## 先に settings_reset を当てる

このリポジトリは ZMK Studio 有効（`CONFIG_ZMK_STUDIO=y`）のため、**過去に ZMK Studio や
keymap editor で本体側に保存したキーマップが、ファームを焼き直しても残る**。
配布先で「このブランチの配列と違う」となる場合は、左右それぞれに
`settings_reset` の uf2 を先に書き込んでから、上記の左右ファームを書き込む。
Bluetooth のペアリング情報も消えるので、その後ホストとペアリングし直す。

## 動作確認のチェックリスト

- [ ] 左手 21 キー / 右手 21 キーがすべて反応する（レイヤー0 だけで全キーが文字か修飾キーを出す）
- [ ] `Ent*` `Spc*` をタップすると Enter / Space、長押し中に記号・数字が出る
- [ ] 親指の `英数*` `かな*` をタップして日本語 / 英語が切り替わる（**Windows と Mac の両方で**）
- [ ] 切り替わらない環境では、親指キー長押し中の内側キー（無変換 / 変換）で切り替わる
- [ ] 左右どちらの親指キー長押しでもカーソルキーが使える
- [ ] 打鍵しながらボールに触れても、文字がクリックに化けない
- [ ] 両親指同時押し → layer_4 で `&bt BT_SEL 0`〜`4`、`&bt BT_CLR` が効く
- [ ] 右手側トラックボールでポインタが動く（COROPIT 版として正しい向きか。反転していたら下記参照）
- [ ] ボールを動かした直後に `F` または `J` で左クリック、`D` または `K` で右クリックできる
- [ ] 左手側ロータリーエンコーダーでスクロールする
- [ ] 左右が BLE で接続され、電池残量が OS 側に見える
- [ ] ホストの Bluetooth 一覧に `mona2` が出て、パスコードなしで接続できる
- [ ] 渡す前に `&bt BT_CLR_ALL` で自分のペアリングを消した

## トラックボールは COROPIT 版前提

このブランチ（および `main`）の `boards/shields/mona2/mona2_r.overlay` は、
**COROPIT 版トラックボールモジュール向けに `invert-x` / `invert-y` を有効にした状態**。
COROPIT 付きの個体にそのまま焼けば、ポインタの上下左右は正しい向きで動く。

自作 PMW3610 など COROPIT 以外のモジュールに載せ替える場合は、この2行を
コメントアウトに戻す（README の「COROPIT を使用する方へ」の逆操作）。

```
        cpi = <600>;
        //swap-xy;
        invert-x; //COROPIT版ではコメントアウトを外す
        invert-y; //COROPIT版ではコメントアウトを外す
```
